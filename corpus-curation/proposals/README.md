# Curation proposals

Place LLM-generated curation proposal JSON files here. Proposal files contain locators, hashes, and question mappings only; they must not contain copied canonical source text.

A proposal is not trusted corpus data until `sibyl-corpus import-curation` validates it against the local prepared canonical source.
