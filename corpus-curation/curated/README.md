# Validated curation mappings

This directory is the intended Git-tracked destination for normalized LLM-curated passage/question mappings produced by `sibyl-corpus import-curation`.

The files contain source identities, exact locators, hashes, deterministic curated passage IDs, word counts, and question-match strengths. They do not contain the literary passage text itself.
